实现一个app，将鸿蒙手机模拟成蓝牙HID输入设备，用于将连接到鸿蒙手机的gamepad输入转换为hid触控或鼠标输入，实现将gamepad摇杆输入转换为触屏摇杆输入。
请在 C:\Program Files\Huawei\DevEco Studio 查找鸿蒙SDK，寻找可用的API。
请考虑要模拟成鼠标、触控板还是其他相对/绝对定位HID设备，以便可以向屏幕的特定位置发送触控/点击输入。
需要模拟触控的应用布局：
"C:\Users\hu60\Pictures\screenshot_20260429_063026_com.hottagames.yh.huawei.jpg"
我们需要首先实现将gamepad左摇杆输入转换为对屏幕左下角虚拟摇杆的触控输入。

------------------------

你理解错了。我们需要做的事情是通过蓝牙模拟HID设备，实现对远程应用的点击，所以不涉及无障碍触控模拟，也不涉及本地触控事件注入。
"C:\Users\hu60\Pictures\screenshot_20260429_063026_com.hottagames.yh.huawei.jpg"是远程应用的布局，我们是要通过蓝牙HID实现虚拟摇杆触摸。
请继续规划应用实现方法。

------------------------

我希望修改计划，先尝试绝对位移，如果不成功再考虑相对位移

------------------------

请继续，完整实现从设备发现、配对到连接、发送的全过程

------------------------

我按Register HID好像没有任何反应。请查看 C:\work\BtGamePad\hdc.log 分析原因

------------------------

我点了，Last Error并没有出现任何内容

------------------------

我现在可以连上host了，也作为输入设备完成配对了，并且点Send Touch也会有反应，host的鼠标会隐藏，进入触控模式。但是触控最后没有被触发。请检查日志看看有什么问题。

------------------------

我再次进行了验证，请检查日志

------------------------

请开始面向最终用户重构界面：
首先是选择蓝牙设备，此阶段应该自动打开蓝牙开关、注册HID设备，并打开设备发现。如果失败则弹出错误提示写明原因，并尝试关闭蓝牙开关然后重新打开，然后重新注册HID设备和打开设备发现。

------------------------

继续重构，只保留两个按钮，一个是忘记已配对设备，另一个是连接。点连接后如果没配对就自动完成配对。

------------------------

既然没有，就移除忘记设备功能，只保留连接功能

------------------------

继续修改：
1. 点连接并连上后，按钮应该变成断开。
2. 配对状态只保留右上角角标，不需要单独一行展示。
3. 信号强度也改成角标图标形式，可以用系统自带的信号或者wifi图标。不需要单独一行展示。

------------------------

目前对信号的判断是不是有问题，完全没开机的设备也显示信号满格

------------------------

请继续修改。如果连接时得到正在配对中的错误，就应该显示配对中，然后继续等待配对完成再连接，而非显示错误信息。此外，显示过一次错误信息之后，以后每次连接都会显示重复的错误信息。还有，似乎不管有没有连上，连接按钮都会变成断开。应该有一个连接中的状态。连接中和配对中点了都做成断开的效果。最后，我希望不在线的设备的信号图标是离线状态，而非0格。

------------------------

还是不对。Matebook Pro是在线设备，我甚至都连上了，但它的信号还是离线状态。请检查日志分析问题原因。此外，我希望点击信号图标后显示具体的RSSI，点击已配对图标后显示具体的配对详情。

------------------------

我想我知道原因了。后端会定期更新RSSI，但前端没有把新的RSSI显示出来。请移除前端排序，只保留后端排序，并保证新的RSSI和配对状态会得到更新。

------------------------

你说的“同名合并”是什么意思，按名称合并设备？这绝对不可以，同名但不同mac的设备是大量存在的。

------------------------

我发现准备功能存在判断错误，明明HID注册成功了甚至可以连上设备，但状态仍然是未完成。设备发现也打开了，但状态是未完成。

------------------------

hdc.log清晰显示我运行的就是当前的包。现在还是有问题就是你的问题。请不要逃避，继续进行修复。

------------------------

最近状态和最近错误没必要显示了。我更改了文案，#sym:discoveryReady 现在叫“启用设备发现”。现在注册HID的状态正确了，设备发现的状态还是不正确。我明明看到了未配对设备，怎么能说启用设备发现没完成？请检查日志确认问题。

------------------------

把三个状态以及当前选择改成独立的4行，当前选择显示设备名称，点击后显示虚拟MAC地址

------------------------

4个状态行加一些左右边距，不要太靠外

------------------------

还是不好看。能不能改成状态行尽可能朝中线对齐，空间不够再向左右两边展开

------------------------

继续开发后续功能。在蓝牙设备连接成功后，跳转到键位设置页面。该页面允许导入一张截图（比如"C:\Users\hu60\Pictures\screenshot_20260429_063026_com.hottagames.yh.huawei.jpg"）作为窗口背景，用来定位控件位置。截图也用来确定目标设备的屏幕分辨率。如果截图是横屏就让窗口变成横屏，如果截图是竖屏就让窗口变成竖屏，然后也提供横竖屏手动切换按钮。此外，允许添加控件，控件包括XBox游戏手柄的所有按键、扳机、摇杆，以及键盘上的所有按键，以及键盘行走和键盘视角两类特殊按键。添加后，可以拖放到页面上，然后点击保存。保存之后，玩家点击手柄和键盘上的对应按键/摇杆/扳机，就在对应的位置触发触摸事件。要实现多个布局保存和切换，并且每个布局对应的背景图也要保存。

------------------------

继续开发后续功能。在蓝牙设备连接成功后，跳转到键位设置页面。该页面允许导入一张截图（比如"C:\Users\hu60\Pictures\screenshot_20260429_063026_com.hottagames.yh.huawei.jpg"）作为窗口背景，用来定位控件位置。截图也用来确定目标设备的屏幕分辨率。如果截图是横屏就让窗口变成横屏，如果截图是竖屏就让窗口变成竖屏，然后也提供横竖屏手动切换按钮。此外，允许添加控件，控件包括XBox游戏手柄的所有按键、扳机、摇杆，以及键盘上的所有按键，以及键盘行走和键盘视角两类特殊按键。添加后，可以拖放到页面上，然后点击保存。保存之后，玩家点击手柄和键盘上的对应按键/摇杆/扳机，就在对应的位置触发触摸事件。要实现多个布局保存和切换，并且每个布局对应的背景图也要保存。

------------------------

你不应该把原生输入和布局仓库放在设备页，请把它们移动到键位布局准备页。还有，连上后，点击返回设备页之后应该有切换到键位布局准备页的按钮。

------------------------

添加控件的布局不应该是这样的，应该是全屏，然后点击按钮展开控件库，选择之后控件出现在默认位置，然后可以拖动到我想要的位置

------------------------

问题：
1. 这个背景图是横屏，app应该也进入横屏模式，让截图尽可能全屏显示。
2. 横屏时菜单按钮应该在左右两侧，竖屏时在上下两侧，不应该遮挡背景图。

------------------------

问题：
1. 菜单还是应该叠加在画布上，让背景图尽可能全屏。建议改成用一个下拉按钮展开操作菜单，然后下拉按钮可以拖动。
2. 控件应该只有这几种类型：摇杆移动、摇杆视角、键盘移动、键盘视角、按钮、滑动。把控件拖放到画布上之后，再点击按钮绑定具体的按键或轴。
3. 目前添加到画布上的按钮无法移动，请修复。

------------------------

不对，问题应该是坐标记录了更新，但按钮没有跟随更新，不是手指离开按钮范围。重启应用后两个按钮确实到了新的位置，说明移动位置被记录下来了，只是移动的时候按钮位置没有随着更新。

------------------------

请继续重构：
1. 摇杆移动、视角、按钮应该是圆形。移动中间加四向箭头图标，视角中间加摄像机图标。
2. 滑动应该是长条形，左右各有一个调整点，拖动即可自由拉伸和改变角度。
3. 通过手势进行大小缩放。
4. 别通过菜单选择绑定的按钮，应该是选中后进入监听状态，然后按手柄/摇杆/键盘/鼠标来绑定。
5. 滑动可以绑定线性扳机和摇杆，如果绑定摇杆，应该绑定到具体的轴（X/Y轴）上。

------------------------

移动和按钮控件应该是正圆形，不应该是圆角矩形。还有，请把绑定键位文字放在控件下方，不要放在控件内。控件内仅放按钮名称。最后，请允许移动、视角和按钮调到更小，目前的最小大小还是太大了。此外，只要选中控件后，应该无论在哪里缩放都要能调整大小。

------------------------

空白处缩放生效了。
目前的问题：
1. 滑动控件的框不见了。
2. 按钮和移动还是圆角矩形，不是圆形，并且不能缩到很小。
3. PS风格按键（L1/L2/R1/R2等）没有正确缩短。
4. 按钮比较小时绑定的按键名称很早就省略了。最好改成不会省略。
5. 写一个通用的规则用来处理按键名称，比如截去KEYCODE_BUTTON_前缀作为名称

------------------------

继续重构：
1. 键盘移动和键盘视角应该要绑定4个按键，分别对应上左下右。监听开始后连续按4个键后结束。
2. 在拖动滑动控件的左调整点时不应该让右调整点有位移，反之亦然。
3. 每种控件的编号应该独立，按钮1、摇杆移动1等。
4. 现在所有控件的默认尺寸都太大了，请参考"C:\Users\hu60\Pictures\screenshot_20260429_063026_com.hottagames.yh.huawei.jpg"调整到合适的大小。

------------------------

继续修复：
1. 滑动条使用两个调整点的位置定位，移动中间控件时同步调整两个调整点的坐标，移动单个控制点时另一控制点不动。此外，不改变文字朝向，改为在条上方绘制箭头标示滑动方向。
2. 允许在空白处滑动调整选中控件的位置。
3. 按键名称离控件太远了，间距调小一点。
4. 按键未绑定时选中后显示提示文字，比如 按下按键绑定 / 晃动摇杆绑定 / 连按四键绑定 / 推动摇杆或扳机绑定

------------------------

继续重构：
1. 选中时在菜单里提示绑定方法的同时也直接显示在控件下方（取代“未绑定”提示）。
2. 滑动应该只有一个方向，从白点滑动到黄点，不应该显示双向箭头。
3. 滑动调整其中一个点时，另一个点还是会移动，请仔细检查原因。我已经在 C:\work\BtGamePad\hdc.log 中记录了滑动过程。

------------------------

继续重构：
1. 摇杆视角和键盘视角都要是圆角矩形，并且有摄像机图标。
2. 摇杆移动和键盘移动都要是圆形，并且有四向箭头图标。
3. 绑定方向键右的时候有时候名称是“方向右”，有时候名称是“RIGHT”，为什么？是不是两个来源的输入有重复？
4. 滑动条移动一侧之后另一侧也动的问题依然没有解决。

------------------------

为什么会出现中文和英文两个源，它们分别来自哪里？是不是说同一次按键会从gamepad api和normal input api那边收到两次输入？请通过 C:\work\BtGamePad\hdc.log 确认。

------------------------

但有问题的控件并不是键盘移动和键盘视角，而是普通按钮。之前我普通按钮第一次绑定是中文，清除后第二次绑定就是英文，是随机的。其他按键也存在同样的问题，不止是方向键。你应该在日志中仔细记录，以便调试问题。

------------------------

我已经确定了某些按键会同时从GameControllerKit和KeyEvent收到，比如方向键，以及手柄的普通按钮。请统一两边的按钮名称。比如手柄右摇杆按下有时候叫R3，有时候叫RIGHTTHUMBSTICK，非常奇怪。
还有，归一化未知名称时不应该去除下划线，应该把下划线改为空格。

------------------------

请总是总是总是总是使用原始keycode或者类似的原始代码进行匹配，不要对用于匹配的标识符进行任何处理！否则我们可能无法区分键盘X和手柄X！
只对显示名称进行处理，并且解决手柄XYAB可能显示XYAB或者BUTTONX/BUTTONY/BUTTONA/BUTTONB这种不一致问题。

------------------------

把按钮匹配完全切到 raw source + raw code，不再依赖处理过的名字。

------------------------

请移除所有向后兼容代码。我们还在原型阶段，不需要考虑任何配置文件的向后兼容。

------------------------

开始实现执行功能。在按键编辑菜单添加一个保存按钮，保存后进入执行模式，将按键和摇杆输入转换为数位板输出通过蓝牙发送，并在屏幕上显示可视化效果（比如点击的位置）。屏幕上的控件也需要保留。

------------------------

请修复以下问题：
1. 摇杆的Y轴方向与实际相反
2. 坐标似乎计算错了，应该以截图的分辨率和控件在截图上的相对位置为计算基准，而非控件在屏幕上的相对位置。此外，存储控件位置时也应该使用控件在截图上的相对位置存储，以便导入不同屏幕分辨率的设备后位置都保持正确。

------------------------

请删除已有旧布局则会在读取到截图尺寸后自动做一次迁移并保存的代码。我们目前处于原型阶段，不需要考虑向后兼容

------------------------

摇杆移动的运动范围不正确，是一个靠上的椭圆，并没有覆盖完整的圆形范围。请改为覆盖完整的圆形范围。键盘移动如果也有类似问题，也请修复。

------------------------

完全不对！摇杆移动、键盘移动、按钮现在都变成矩形了，不是圆形了！

------------------------

你没有修复问题，现在摇杆移动的轨迹还是靠上的椭圆。而且按钮的点击位置也不正确，现在是在控件圆形的顶部触发，不是在控件圆形的中心

------------------------

摇杆移动在可视化方面正确了。但是执行时的坐标变换还是不正确，现在看起来更像以手机屏幕为坐标但是横向压扁了。不应该是这样。应该是以截图为坐标，超出截图的部分就忽略，没超出的部分保留，并且不进行坐标挤压。

------------------------

可视化层也有问题，目前显示的“位置”属性是以手机屏幕左上角为坐标原点，应该改成以截图左上角为坐标原点，超出截图的部分用负数表示。请在持久化上直接进行调整，不需要考虑向后兼容性。

------------------------

我是说我希望我们内部存储位置时，使用截图的左上角作为坐标原点，而不是仅仅在显示的时候。你应该知道，截图在手机上显示时，左右两边是有黑边的。所以要进行正确的计算。

------------------------

不对，你还是把坐标原点算错了。截图是以“适应屏幕”的方式显示的，所以截图坐标原点并不是在屏幕左上角。你现在的坐标原点还是在屏幕左上角。请注意：现在是左右有黑边的情况。如果上下有黑边左右没有，又是另一种情况。要同时正确处理两种情况。

------------------------

不存在“不是新坐标”的问题，这个控件是我新增的。你还没发现你的真正错误吗？对于左右有黑边的情况，截图坐标原点的横坐标是屏幕坐标原点加上左侧黑边的长度！

------------------------

你读取截图尺寸失败了，请检查 C:\work\BtGamePad\hdc-hilog.log 

------------------------

问题没有解决。请继续看C:\work\BtGamePad\hdc-hilog.log

------------------------

不要修正旧值。我们不考虑向后兼容

------------------------

还是不行。你是不是搞错API了，你用的API真的能达到目的吗？

------------------------

每次重新打开app，控件的尺寸都会变大。请修复该问题。
C:\Program Files\Huawei\DevEco Studio

------------------------

请为app适配深色模式

------------------------

请修复构建错误
SDK：
C:\Program Files\Huawei\DevEco Studio

------------------------

请继续修复构建错误，直到 hvigorw assembleHap 构建正常。我已经设置好环境变量，现在只需要直接运行 hvigorw assembleHap 就能构建

------------------------

请为app实现键盘移动和键盘视角功能。要实现八向移动，即两个方向键合成45度移动

------------------------

请通过C:\work\BtGamePad\build-hap.bat进行构建并修复构建错误

------------------------

实现的不对，按下一个方向键后应该从圆心向目标方向滑动，按下第二个方向键后应该从第一个方向键位置向45度位置滑动

------------------------

还需要调整。刚按下第一个方向键时，点击首先应该出现在圆心，然后迅速平滑移动到目标位置。此外，在任何时候放开方向键都应该直接释放触控，不应该回落到圆心。

------------------------

#sym:getMouseButtonLabel 的映射不正确，与实际不符，请修复。比如中键应该是左键，扩展键应该是右键。请核对SDK中的常量。

------------------------

执行的时候是不是也有问题，目前似乎鼠标点击无法触发

------------------------

修改摇杆视角的实现方法，改为从原点出发不断向摇杆指向的方向缓慢移动，摇杆的幅度就是移动的速度，如果超出屏幕就松开触摸，再次回到原点继续移动，直到松开摇杆，就直接松开触摸，不回到原点。

------------------------

添加两个新选项：
1. 竖屏设备（发送坐标时交换X/Y值）
2. 翻转设备（发送坐标时用W-X，H-Y）

------------------------

目前的问题：
开启竖屏设备后，本来应该跑到左下角的点击却跑到左上角去了。
开启翻转设备后，本来应该跑到右下角的点击却跑到右上角去了。
请修复上述两个坐标映射错误（是我之前说错坐标变换方法了，请按我刚刚提到的线索推到正确的变换方法）。

------------------------

翻转设备是我搞错了，应该改回上一个实现

------------------------

请为模拟鼠标也添加相同的坐标变换，让竖屏设备和翻转设备选项变成两个模式都可用
